<div class="text-center">
  <h2>{{title}}</h2>
  <p>ISBN: {{ISBN}}</p>
  <p>Written by {{author}} </p>
  <p>Category: {{category.categoryName}}</p>
  <p>
    <button class="btn btn-primary">Checkout {{title}}</button>
  </p>
</div>
